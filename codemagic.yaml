workflows:
  watchmusic-unsigned:
    name: WatchMusic - Unsigned Build (free sideload)
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      xcode: latest
      vars:
        XCODE_PROJECT: "WatchMusic.xcodeproj"
        XCODE_SCHEME: "WatchMusic"
    scripts:
      - name: Install XcodeGen
        script: |
          brew install xcodegen
      - name: Generate Xcode project from project.yml
        script: |
          cd $CM_BUILD_DIR
          xcodegen generate
      - name: Build unsigned .app (embeds the Watch app automatically)
        script: |
          cd $CM_BUILD_DIR
          xcodebuild \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            build
      - name: Package as unsigned IPA
        script: |
          cd $CM_BUILD_DIR
          APP_PATH=$(find build/Build/Products -maxdepth 2 -name "WatchMusic.app" -not -path "*simulator*" | head -n 1)
          if [ -z "$APP_PATH" ]; then
            echo "ERROR: could not find WatchMusic.app in the build output."
            echo "Contents of build/Build/Products:"
            find build/Build/Products -maxdepth 3
            exit 1
          fi
          echo "Found built app at: $APP_PATH"
          rm -rf Payload
          mkdir -p Payload
          cp -R "$APP_PATH" Payload/
          zip -r WatchMusic-unsigned.ipa Payload
          echo "IPA contents:"
          unzip -l WatchMusic-unsigned.ipa
          ls -la WatchMusic-unsigned.ipa
    artifacts:
      - "*.ipa"
      - build/Build/Products/Release-iphoneos/*.app