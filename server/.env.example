PORT=3000

# Leave these two blank/unset to disable auth entirely (fine if the server
# only listens on your home LAN). Set them if you're exposing it through a
# Cloudflare Tunnel or anything else reachable from the internet.
AUTH_USERNAME=
AUTH_PASSWORD=
